{
  "description": "burn-rate plugin Stop hook — snapshots Claude Code session usage to SQLite, updates statusline sentinel. Requires Python 3.11+. See hooks/log-usage.py for details.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/log-usage.py\"",
            "async": true
          }
        ]
      }
    ]
  }
}
